@model IEnumerable<WebApplication.Models.Skladiste>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Dodavanje ", "Create" , null, new { @class="create-button"})
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Kolicina)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DodatniTretmani.NazivTretmana)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Kolicina)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DodatniTretmani.NazivTretmana)
        </td>
        <td>
            @Html.ActionLink("Izmjeni", "Edit", new { id=item.SkladisteID }, new { @class = "edit-button" }) 
            @Html.ActionLink("Vidi više", "Details", new { id=item.SkladisteID }, new { @class = "details-button" }) 
            @Html.ActionLink("Obriši", "Delete", new { id=item.SkladisteID }, new { @class = "delete-button" })
        </td>
    </tr>
}

</table>
