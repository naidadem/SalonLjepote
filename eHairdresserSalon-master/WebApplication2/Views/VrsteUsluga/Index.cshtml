@model IEnumerable<WebApplication.Models.VrsteUsluga>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Dodavanje ", "Create" , null, new { @class="create-button"})
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Naziv)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Naziv)
        </td>
        <td>
            @Html.ActionLink("Izmjeni", "Edit", new { id=item.VrstaUslugeID }, new { @class = "edit-button" }) 
            @Html.ActionLink("Vidi više", "Details", new { id=item.VrstaUslugeID }, new { @class = "details-button" }) 
            @Html.ActionLink("Obriši", "Delete", new { id=item.VrstaUslugeID }, new { @class = "delete-button" })
        </td>
    </tr>
}

</table>
